-- Wakili PostgreSQL initialization
-- Creates the wakili database if it doesn't exist
-- Tables are created by SQLAlchemy on startup (or Alembic in production)
SELECT 'Wakili DB initialized' AS status;
