FROM node:20-alpine
WORKDIR /app

# Copy package files first for better caching
COPY package.json package-lock.json* ./
RUN npm install

# Copy the rest of the source code (will be overridden by volume mount anyway)
COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev"]